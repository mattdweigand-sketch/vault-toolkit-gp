# Market Thesis References

<!--
This folder holds knowledge that carries across thesis cycles, not the inputs to
one thesis (those are L4, in the stage outputs).

What lives here:
- Prior theses, by market/sector, with their tracked calls — what each predicted
  and the indicators to watch. The synthesis stage reads these to update a view
  rather than restart it, and they let the firm see which calls held up.
- The evidence library: durable, reusable market facts and context worth keeping
  across cycles (not the time-stamped data points of a single research pack).
- Standing market context the firm wants every thesis to account for.

This is L3 reference, shared across theses. Keeping prior theses and their calls
here is what keeps the firm honest about its own forecasting — a thesis function
with no memory of its past calls cannot tell whether it is any good. It is not a
learning loop (the workflow does not run to grow this store), but the tracked
calls give the firm a record to check itself against.
-->
