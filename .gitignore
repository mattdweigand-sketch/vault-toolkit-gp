# macOS
.DS_Store
**/.DS_Store
._*
**/._*

# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# Setup state runtime lock
_shared-config/setup-session.lock

# Local setup testing clone
_sandbox/
