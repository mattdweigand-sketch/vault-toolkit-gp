# Builder Variants

Archived builders for the older lifecycle templates. Keep these for reference and migration, but default setup routing should use the active builders in the parent `skill-starters/` folder.
