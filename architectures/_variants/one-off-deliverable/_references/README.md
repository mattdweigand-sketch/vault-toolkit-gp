# One-Off Deliverable References

<!--
This folder holds knowledge that applies beyond this single deliverable, not
the source set for it (that lives in 00_sources/) and not this deliverable's
own config (that lives in _config/).

What lives here:
- House style or voice guidance the draft should follow.
- Prior comparable deliverables, for format and consistency.
- Standing standards that apply regardless of this particular source set
  (underwriting standards, disclosure language, a firm's citation conventions).

This is L3 reference, and it starts empty. For a true one-off it often stays
empty: the deliverable is grounded in 00_sources/, not in cross-deliverable
knowledge. Populate it only when something genuinely carries across deliverables.

House voice is the usual standing reference — but it is firm-wide, so it does not
get copied in here. Point at the shared firm voice file,
`_shared-config/voice-and-tone.md` (from this workspace under workspaces/<name>/,
that is ../../_shared-config/voice-and-tone.md). Use this folder only for material
genuinely specific to this kind of deliverable that still carries across runs. A
single standing reference like voice does NOT mean the work is recurring.

What does signal recurring work is this folder filling with material specific to
*this kind of deliverable* that you rebuild every time — templates, a standing
comp set, a fixed report skeleton. That is a cycle, not a one-off. Move it to a
document-production workflow (lp-reporting, asset-management) where the cycle is
the point and shared references are first-class.
-->
