# Reusable Prompts

<!--
This folder holds reusable prompt fragments for the recurring analytical tasks
in this workspace. These are NOT copy-paste prompts. They are structured
instruction fragments the review stage loads when running a standard analysis,
so the same task is done the same way each cycle.

Examples:
- Variance attribution: a fragment that walks the model through separating a
  timing variance from a structural one and stating the cause.
- Watchlist triage: a fragment that applies the review-standards thresholds and
  drafts the watchlist entry (trigger, cause, action, owner).
- Hold/sell/refi signal framing: a fragment that frames a data signal as an
  input for the IC without crossing into the return math (Constraint 09).

Keep each fragment short and focused on one analytical motion. Add one when you
find yourself giving the model the same analysis instructions more than a few
times.
-->
